import styled from '@emotion/styled';

export const HeaderContent = styled.div(({ styles }: any) => ({
	width: '100%',
	height: '13vh',
	background: ' white',
	boxShadow: '0px 5px 15px rgba(0, 0, 0, 0.1)',
}));

export const HeaderLogo = styled.img(({ styles }: any) => ({}));
