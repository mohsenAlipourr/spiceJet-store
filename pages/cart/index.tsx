import styled from '@emotion/styled';

const Button = styled.button`
	color: turquoise;
`;

const Cart = () => {
	return (
		<div>
			<Button>This my button component.</Button>
		</div>
	);
};

export default Cart;
